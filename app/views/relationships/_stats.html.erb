<% @user ||= current_user %>
<div class="stats">
  <a href="<%= following_relationship_path(@user) %>">
    <strong id="following" class="stat">
      <%= @user.following.count %>
    </strong>
    following
  </a>
  <p />
  <a href="<%= followers_relationship_path(@user) %>">
    <strong id="followers" class="stat">
      <%= @user.followers.count %>
    </strong>
    followers
  </a>
</div>